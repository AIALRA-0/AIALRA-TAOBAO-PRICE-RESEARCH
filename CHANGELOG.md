# 变更记录

这份文件记录使用者能够感知的版本变化

每次发布时填写日期，并按新增、修改、修复、弃用、安全或破坏性变化分类

## [0.6.0] - 2026-07-29

### 修复

- 新增 `identity_phrases`，要求商品标题命中至少一个连续型号写法
- 商品仍须包含全部 `required_terms`，型号和规格词散落在无关标题中时不能进入详情候选
- 增加身份词散落干扰的回归测试

## [0.5.3] - 2026-07-28

### 修复

- 详情候选必须同时包含全部商品身份词，避免缺少容量或规格的错误商品进入最低价核验
- 增加缺少部分商品身份词的回归测试

## [0.5.2] - 2026-07-28

### 稳定性

- 等待确认、等待浏览器结果和等待用户期间不再消耗 Runner 总时限
- 任务中断后可以原地续接当前浏览器节点，并保留已经完成的节点结果

### 兼容性

- 旧运行状态继续时自动补充活跃时间记录

## [0.5.1] - 2026-07-27

### 结构

- 后端条目统一进入 `source_routing.providers` 大分类
- 后端选择和硬停止规则统一进入 `source_routing.selection_policy`
- 明确声明 MCP 准确标识、动作白名单和观察证据协议

## [0.5.0] - 2026-07-27

### 新增

- 接入 `AIALRA Shopping Browser` 本地持久 Chrome MCP
- 搜索、详情和最终结果记录实际浏览器后端
- 增加通用观察证据与淘宝平台 Schema 的双层校验
- 增加浏览器后端路由和第三方采用说明

### 修改

- 外部采集节点从内置浏览器改为 MCP 执行器
- 浏览器登录状态改由仓库外的独立 Chrome 资料保存
- 安装新插件后必须在新任务中重新完成淘宝访问预检

### 安全

- 不导出 Cookie、存储状态、密码或验证码
- 当前任务命中 `policy-blocked` 后仍然保持硬停止

## [0.4.0] - 2026-07-26

### 稳定性

- 搜索和详情改为单页面串行读取
- 相邻自动动作至少间隔三秒
- 查询最多三个且每个查询最多读取一页
- 详情核验最多八个商品，评价核验最多三个商品
- 同一次运行复用十五分钟内已经取得的查询和详情证据

### 安全

- 登录、人机检查、限流和宿主策略阻止出现后不自动重试
- `policy-blocked` 成为 Runner 硬停止，不能进入回退或更换执行面

### 测试

- 增加浏览器节点单通道、动作间隔、缓存和零风险重试契约测试

## [0.3.1] - 2026-07-26

### 兼容性

- 固定使用 `actions/checkout v7.0.1`，消除 Node.js 20 弃用警告
- 移除会自动创建 PR 的 Dependabot 配置，依赖更新改为人工审计后直接推送主分支

## [0.3.0] - 2026-07-24

### 修改

- 商品采集只接受淘宝搜索页和具体商品页
- 浏览器控制面在第一次访问前确定并在本次运行中保持不变
- 宿主策略拒绝淘宝网址时直接结束为失败，不生成替代价格结论

### 移除

- 移除淘宝开放平台候选搜索和详情接口节点
- 移除人工入口和有限证据完成节点
- 移除已经没有节点使用的回退输入 Schema

## [0.2.0] - 2026-07-24

### 新增

- 接入淘宝客物料搜索升级版作为浏览器搜索失败后的只读候选通道
- 接入淘宝客商品详情升级版作为详情页失败后的 SKU 价格与库存通道
- 加入淘宝开放平台 HMAC-SHA256 HMAC-MD5 和 MD5 请求签名
- 加入官方接口响应规范化 覆盖警告 凭据缺失回退和脱敏错误处理
- 加入官方接口接入文档和凭据安全边界
- 加入官方签名 搜索响应 详情响应和工作流回退测试

### 修改

- 浏览器策略阻止先进入官方接口 官方接口也失败后再进入人工回退
- A 级证据允许来自具体商品页或淘宝客商品详情升级版的目标 SKU 价格
- 官方接口缺失的评价 退货 保修 店铺评分和费用保持未知

## [0.1.0] - 2026-07-24

### 新增

- 从通用图驱动模板生成独立 Skill 仓库
- 加入安全草稿、Runner、Schema、测试、核心锁和受控学习结构
- 加入淘宝多搜索词批量候选采集协议
- 加入详情页目标 SKU 店铺 图片 评价 退货和保修证据契约
- 加入链接去跟踪 去重 明显错误商品排除和详情候选排序
- 加入确定性成本计算 风险评分 可行性判断和最低价赢家复算
- 加入浏览器登录暂停 人工回退 覆盖说明和手动搜索入口
- 加入正常完成 错误证据 虚假赢家 登录暂停和安全边界测试
- 同步 Runtime 的节点输入再次检查和 `policy-blocked` 失败类型
- 记录当前 Codex 浏览器策略阻止淘宝访问以及已验证的安全回退结果
- 增加保留完整仓库 Runtime 的本地符号链接安装器
- 排除核心锁 SHA-256 摘要的 Gitleaks 误报
